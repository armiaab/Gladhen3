<ContentDialog
    x:Class="Gladhen3.Dialogs.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gladhen3.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="SettingsDialog"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    DefaultButton="Primary"
    CloseButtonStyle="{StaticResource DefaultButtonStyle}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="400">
        <StackPanel Spacing="16">
            <StackPanel>
                <TextBlock x:Uid="SettingsDialog_PaperSizeLabel" FontWeight="SemiBold" />
                <ComboBox x:Name="PaperSizeCombo" Width="220" SelectionChanged="PaperSizeCombo_SelectionChanged">
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_Auto" />
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_A4" />
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_Letter" />
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_Legal" />
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_A3" />
                    <ComboBoxItem x:Uid="SettingsDialog_PaperSize_Custom" />
                </ComboBox>
            </StackPanel>

            <StackPanel x:Name="CustomSizePanel" Spacing="8" Margin="0,8,0,0" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Uid="SettingsDialog_UnitLabel" VerticalAlignment="Center" Width="50" />
                    <ComboBox x:Name="UnitCombo" Width="120" SelectionChanged="UnitCombo_SelectionChanged">
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_MM" />
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_IN" />
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_PT" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Uid="SettingsDialog_WidthLabel" VerticalAlignment="Center" Width="50" />
                    <NumberBox x:Name="WidthBox" Minimum="1" Maximum="10000" SpinButtonPlacementMode="Compact" Width="120" SmallChange="1" LargeChange="10" />
                    <TextBlock x:Name="WidthUnitLabel" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Uid="SettingsDialog_HeightLabel" VerticalAlignment="Center" Width="50" />
                    <NumberBox x:Name="HeightBox" Minimum="1" Maximum="10000" SpinButtonPlacementMode="Compact" Width="120" SmallChange="1" LargeChange="10" />
                    <TextBlock x:Name="HeightUnitLabel" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,4,0,0">
                    <TextBlock x:Uid="SettingsDialog_PresetsLabel" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Button Content="A4" FontSize="11" Padding="8,4" Click="PresetA4_Click" />
                    <Button Content="Letter" FontSize="11" Padding="8,4" Click="PresetLetter_Click" />
                    <Button Content="A5" FontSize="11" Padding="8,4" Click="PresetA5_Click" />
                    <Button Content="4×6&quot;" FontSize="11" Padding="8,4" Click="Preset4x6_Click" />
                </StackPanel>
            </StackPanel>

            <StackPanel>
                <TextBlock x:Uid="SettingsDialog_OrientationLabel" FontWeight="SemiBold" Margin="0,4,0,0" />
                <ComboBox x:Name="OrientationCombo" Width="220">
                    <ComboBoxItem x:Uid="SettingsDialog_Orientation_Auto" />
                    <ComboBoxItem x:Uid="SettingsDialog_Orientation_Portrait" />
                    <ComboBoxItem x:Uid="SettingsDialog_Orientation_Landscape" />
                </ComboBox>
            </StackPanel>

            <StackPanel>
                <TextBlock x:Uid="SettingsDialog_MarginsLabel" FontWeight="SemiBold" Margin="0,4,0,0" />
                <ComboBox x:Name="MarginCombo" Width="220" SelectionChanged="MarginCombo_SelectionChanged">
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_None" />
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_Narrow" />
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_Normal" />
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_Wide" />
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_ExtraWide" />
                    <ComboBoxItem x:Uid="SettingsDialog_Margins_Custom" />
                </ComboBox>
            </StackPanel>

            <StackPanel x:Name="CustomMarginPanel" Spacing="8" Margin="0,8,0,0" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Uid="SettingsDialog_UnitLabel" VerticalAlignment="Center" Width="50" />
                    <ComboBox x:Name="MarginUnitCombo" Width="120" SelectionChanged="MarginUnitCombo_SelectionChanged">
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_MM" />
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_IN" />
                        <ComboBoxItem x:Uid="SettingsDialog_Unit_PT" />
                    </ComboBox>
                </StackPanel>

                <Grid ColumnSpacing="8" RowSpacing="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <NumberBox x:Name="LeftMarginBox" x:Uid="SettingsDialog_MarginLeft" Minimum="0" Maximum="100" Width="100" SmallChange="0.1" LargeChange="0.5" Grid.Column="0" />
                    <NumberBox x:Name="TopMarginBox" x:Uid="SettingsDialog_MarginTop" Minimum="0" Maximum="100" Width="100" SmallChange="0.1" LargeChange="0.5" Grid.Column="1" />
                    <NumberBox x:Name="RightMarginBox" x:Uid="SettingsDialog_MarginRight" Minimum="0" Maximum="100" Width="100" SmallChange="0.1" LargeChange="0.5" Grid.Column="2" />
                    <NumberBox x:Name="BottomMarginBox" x:Uid="SettingsDialog_MarginBottom" Minimum="0" Maximum="100" Width="100" SmallChange="0.1" LargeChange="0.5" Grid.Column="3" />
                </Grid>
            </StackPanel>

            <TextBlock x:Uid="SettingsDialog_MarginNote" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />

            <StackPanel>
                <TextBlock x:Uid="SettingsDialog_CompressionLabel" FontWeight="SemiBold" Margin="0,4,0,0" />
                <ComboBox x:Name="CompressionCombo" Width="220">
                    <ComboBoxItem x:Uid="SettingsDialog_Compression_None" />
                    <ComboBoxItem x:Uid="SettingsDialog_Compression_Low" />
                    <ComboBoxItem x:Uid="SettingsDialog_Compression_Medium" />
                    <ComboBoxItem x:Uid="SettingsDialog_Compression_High" />
                </ComboBox>
            </StackPanel>

            <TextBlock x:Uid="SettingsDialog_CompressionNote" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />

        </StackPanel>
    </ScrollViewer>
</ContentDialog>
